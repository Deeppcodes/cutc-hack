# Backboard powers the multi-agent forecasting pipeline.
# Without a key the app runs entirely on the seeded dataset.
BACKBOARD_API_KEY=
BACKBOARD_PROVIDER=openai
BACKBOARD_MODEL=gpt-4o
# Optional override, defaults to https://app.backboard.io/api
# BACKBOARD_BASE_URL=
